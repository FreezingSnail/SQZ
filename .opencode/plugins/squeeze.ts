/**
 * squeeze plugin entry — auto-discovered by opencode from `.opencode/plugins/`.
 * Implementation lives in src/plugin/squeeze.ts (typechecked by `npm run
 * typecheck`); this file is a thin re-export so opencode's loader finds it.
 */
export { SqueezePlugin as default, SqueezePlugin } from "../../src/plugin/squeeze.js";